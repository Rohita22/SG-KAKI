# Copy to .env and fill in. This file (and .env) never gets sent to the browser.
PORT=8080
ALLOWED_ORIGINS=http://localhost:5183
GROQ_API_KEY=
# Groq models. Both routes use the same model by default.
GROQ_MODEL=openai/gpt-oss-20b
GROQ_STRUCTURED_MODEL=openai/gpt-oss-20b
